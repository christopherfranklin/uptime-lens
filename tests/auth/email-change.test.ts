import { describe, test } from "vitest";

describe("Email Change", () => {
  test.todo("auth config has user.changeEmail.enabled set to true");
  test.todo("email change verification uses EmailChangeEmail template");
  test.todo("verification email uses fire-and-forget send pattern");
});
