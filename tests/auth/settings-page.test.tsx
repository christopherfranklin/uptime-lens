import { describe, test } from "vitest";

describe("Settings Page", () => {
  test.todo("renders current email as read-only");
  test.todo("renders new email input for email change");
  test.todo("renders logout button");
  test.todo("logout button triggers logout server action");
});
