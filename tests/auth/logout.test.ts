import { describe, test } from "vitest";

describe("Logout Action", () => {
	test.todo("logout function is exported from auth actions");
	test.todo("logout calls auth.api.signOut with request headers");
	test.todo("logout redirects to / after sign out");
});
